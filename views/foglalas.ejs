<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Foglalas</title>
</head>

<body>
    <form method="post" class="p-4 border rounded shadow-sm bg-light">
        <h3 class="mb-4 text-center">Mozi foglalas</h3>

        <div class="mb-3">
            <label class="form-label">Nev:</label>
            <input type="text" class="form-control" name="nev" placeholder="Nev" required value=<%=data.nev %>><br />
        </div>

        <div class="mb-3">
            <label class="form-label">Email:</label>
            <input type="email" class="form-control" name="email" placeholder="Email" required value=<%=data.email
                %>><br />
        </div>

        <div class="mb-3">
            <label class="form-label">Datum es idopont</label>
            <input type="datetime-local" class="form-control" name="datum" placeholder="Datum es idopont" required
                min="2024-10-17T00:00" value=<%=data.datum %>><br />
        </div>

        <div class="mb-3">
            <label class="form-label">Nezok szama:</label>
            <input type="number" class="form-control" name="nezok" placeholder="Nezok szama" required min="1" max="10"
                value=<%=data.nezok %>><br />
        </div>

        <div class="text-center">
            <input type="submit" class="btn btn-primary" value="Foglalas">
        </div>
        <br />

        <% if (errors.length> 0) { %>
            <div class="alert alert-danger" role="alert">
                <ul class="mb-0">
                    <% for( let error of errors ) { %>
                        <li>
                            <%= error %>
                        </li>
                        <% } %>
                </ul>
            </div>
            <% } %>
    </form>
</body>

</html>